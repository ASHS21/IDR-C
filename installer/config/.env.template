# =============================================================================
# Identity Radar - Environment Configuration
# Generated by the Identity Radar installer
# =============================================================================

# ---------------------------------------------------------------------------
# PostgreSQL Database
# ---------------------------------------------------------------------------
POSTGRES_PASSWORD=__DB_PASSWORD__
DATABASE_URL=postgresql://postgres:__DB_PASSWORD__@db:5432/identity_radar

# ---------------------------------------------------------------------------
# NextAuth.js
# ---------------------------------------------------------------------------
NEXTAUTH_SECRET=__NEXTAUTH_SECRET__
NEXTAUTH_URL=http://localhost:__PORT__

# ---------------------------------------------------------------------------
# AI Provider — Docker Model Runner (local, no API key needed)
# ---------------------------------------------------------------------------
AI_PROVIDER=docker_model_runner
AI_MODEL_ENDPOINT=http://host.docker.internal:12434/engines/qwen3.5/v1/chat/completions

# Fallback: Ollama (if Docker Model Runner is not available)
OLLAMA_URL=
OLLAMA_MODEL=qwen2.5:7b

# Fallback: Anthropic (cloud, requires API key)
ANTHROPIC_API_KEY=

# ---------------------------------------------------------------------------
# Application
# ---------------------------------------------------------------------------
NEXT_PUBLIC_APP_URL=http://localhost:__PORT__
